# Normalization Precedence

1. Use explicit identifiers before parsed identifiers.
2. For work item acceptance criteria, prefer the Microsoft field before custom fallback fields.
3. Preserve null for unknown line anchors instead of inventing `0`.
4. Prefer structured warning lists over inline prose when a field is missing.
