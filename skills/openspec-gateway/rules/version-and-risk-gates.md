# Version and Risk Gates

- `Version:` and `Risk Tier:` must appear within the first 40 lines.
- A spec change with a baseline must change the version string when requirements or acceptance criteria change.
- High and Critical risk tiers require explicit human approval before finalization.
- Major version bumps require explicit human approval.
