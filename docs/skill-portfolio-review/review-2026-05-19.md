# Skill Review Report

**Generated:** 2026-05-19  
**Reviewed by:** skill-architect agent  
**Scope:** All skill packages under `skills/`  
**Skills reviewed:** ado-gateway, caveman, code-quality-engine, delivery-engine, doc-engine, ops-engine, repo-engine, spec-engine, test-engine, thinking-engine

---

## Step 1 — Discovery Map

### Skills directory tree

```
skills/
├── INDEX.md                          ← top-level index file (not a skill directory)
├── ado-gateway/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/
│   │   ├── examples/   (pr-comments-only.json, work-item-only.json, work-item-plus-pr-comments.json)
│   │   ├── schemas/    (ado-openspec-handoff.schema.json)
│   │   └── templates/  (blocker.txt, error.txt, handoff.json)
│   ├── references/
│   │   ├── auth-and-safety.md
│   │   ├── end-to-end.md
│   │   ├── handoff-contract.md
│   │   ├── normalization-rules.md
│   │   ├── rule-order.md
│   │   ├── workflow.md
│   │   └── write-actions.md
│   ├── rules/
│   │   ├── normalization-precedence.md
│   │   ├── read-only-boundary.md
│   │   └── write-boundary.md
│   ├── scripts/  (15 scripts: create-pr-comment.sh, create-pull-request.sh,
│   │              create-work-item.sh, create-work-item-comment.sh,
│   │              emit-handoff.sh, ensure-env.sh, fetch-pr-comments.sh,
│   │              fetch-work-item.sh, format-work-item-mention.sh,
│   │              generate-handoff.sh, normalize-work-item.sh, parse-pr-url.sh,
│   │              resolve-ado-user.sh, validate-handoff.sh, write-guard.sh)
│   └── tests/
│       ├── run-validation.sh
│       └── validation-checklist.md
├── caveman/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/
│   │   ├── examples/  (edge-case.md, happy-path.md)
│   │   └── templates/ (output.md)
│   └── tests/
│       ├── run-validation.sh
│       └── validation-checklist.md
│   [NO rules/, references/, scripts/ — intentional per SKILL.md]
├── code-quality-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (examples.md, workflow.md)
│   ├── rules/ (_sections.md, complexity-reduction.md, correctness-first.md,
│   │          legacy-safety.md, minimal-mutation.md, performance-when-real.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── delivery-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (examples.md, workflow.md)
│   ├── rules/ (_sections.md, critical-path.md, estimate-integrity.md,
│   │          unknown-exposure.md, vertical-slicing.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── doc-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/ [5 files], templates/)
│   ├── references/ (examples.md, workflow.md)
│   ├── rules/ (_sections.md, agentsmd-specificity.md, audience-first.md,
│   │          command-accuracy.md, decision-capture.md, diataxis-mode.md,
│   │          readme-completeness.md, structure-over-prose.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── ops-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (examples.md, workflow.md)
│   ├── rules/ (_sections.md, ci-cd-reproducibility.md, deployment-safety.md,
│   │          kubernetes-safety.md, resilience-rollback.md,
│   │          threat-model-boundaries.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── repo-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (artifact-schema.md, examples.md, workflow.md)
│   ├── rules/ (_sections.md, architecture-map.md, convention-extraction.md,
│   │          entrypoint-detection.md, hotspot-identification.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── spec-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (artifact-schema.md, examples.md, openspec-format.md, workflow.md)
│   ├── rules/ (_sections.md, acceptance-criteria.md, diff-versioning.md,
│   │          handoff-consumption.md, openspec-format.md, policy-gates.md,
│   │          requirement-quality.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
├── test-engine/
│   ├── SKILL.md ✓
│   ├── README.md
│   ├── metadata.json
│   ├── assets/ (examples/, templates/)
│   ├── references/ (examples.md, workflow.md)
│   ├── rules/ (_sections.md, behavior-over-implementation.md, e2e-stability.md,
│   │          edge-cases.md, flaky-test-reduction.md, meaningful-coverage.md)
│   ├── scripts/ (validate-output.sh)
│   └── tests/ (run-validation.sh, validation-checklist.md)
└── thinking-engine/
    ├── SKILL.md ✓
    ├── README.md
    ├── metadata.json
    ├── assets/ (examples/, templates/)
    ├── references/ (examples.md, workflow.md)
    ├── rules/ (_sections.md, assumption-audit.md, decision-readiness.md,
    │          option-generation.md)
    ├── scripts/ (validate-output.sh)
    └── tests/ (run-validation.sh, validation-checklist.md)
```

### Discovery flags

| Flag | Item | Notes |
|------|------|-------|
| ⚠️ Unexpected top-level file | `skills/INDEX.md` | Top-level file in skills/ is not a skill directory; expected only for documentation purposes |
| ℹ️ Non-canonical directories | `tests/`, `README.md`, `metadata.json` | Present in all skills; not in the canonical layout spec, but appear to be a deliberate repo convention |
| ✅ No missing SKILL.md | All 10 skill directories | Every skill directory contains SKILL.md |
| ℹ️ Intentional omission | `caveman` has no `rules/`, `references/`, `scripts/` | Documented explicitly in SKILL.md as intentional |

---

## Step 2 — Per-Skill Reviews

---

### `ado-gateway`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `ado-gateway` |
| Description has Use when / Do not use when | ✅ | Both present and concise (~38 words) |
| `allowed-tools` present and scoped | ⚠️ | `read_file` + `shell` — `shell` is broad; scope is justified by bash script execution requirements |
| Non-standard frontmatter fields | ℹ️ | `title`, `version`, `summary` are extras; harmless but not part of the canonical spec |

**Issues:**
- `allowed-tools` uses `shell` which grants broad execution capability. The risk is mitigated by the explicit write-guard pattern, but no comment in frontmatter explains why `shell` is necessary (e.g., "required for ADO API bash scripts"). A brief comment would clarify intent.
- The description word "narrowly" in "narrowly creating approved Azure DevOps work items" is soft. Replace with "creating only the explicitly supported artifact types." "Approved" is also ambiguous (policy-approved vs. user-confirmed); replace with "user-confirmed."
- Minor: `metadata.json` and `SKILL.md` `avoid_when` sections differ slightly — metadata omits "The task requires changing reviewer votes or branch policies," creating a divergence.

**Suggested frontmatter description fix:**
```yaml
description: >
  Use when fetching, parsing, normalizing, or creating only the explicitly supported
  Azure DevOps work items and pull request artifacts (requires user confirmation for
  writes). Do not use when the task targets GitHub PR review, any destructive or
  approval-related Azure DevOps action, or OpenSpec authoring.
```

#### B — Trigger Precision

**Valid triggers (confirmed):**
- Fetching ADO work items or PR comments
- Parsing an ADO PR URL
- Creating work items, PRs, or PR comments with explicit confirmation

**False positive risk (low):**
- "Converting Azure DevOps data into a deterministic contract for another skill" — vague; could be read as any ADO data transformation, not just this skill's output envelope.

**False negative risk (low):**
- "Updating a work item field" is not listed as an avoid-when item. A user might try it and get no clear rejection signal until deep in the workflow.

**Recommendation:** Add to Avoid When: "Updating, patching, or transitioning existing Azure DevOps work items (these mutations are not supported)."

#### C — Rule Hardness

| Location | Current language | Issue | Fix |
|----------|-----------------|-------|-----|
| SKILL.md Write Workflow step 7 | "supply `--file-path` as a path relative to..." | Missing MUST | "MUST supply `--file-path`" |
| write-boundary.md Checks | "Scripts do not accept arbitrary URLs..." | Acceptable — declarative constraint ✓ | — |
| normalization-precedence.md Checks | "Explicit identifiers win over values parsed from a URL" | Soft — "win over" | "MUST take precedence over" |
| read-only-boundary.md Checks | "Read-mode scripts do not accept mutation flags" | Declarative negative ✓ | — |

**No conflicting rules detected.** The read/write boundary rules are clearly separated and non-overlapping.

#### D — Structure & Simplification

- SKILL.md: 141 lines — within limits ✓
- No dead sections ✓
- **Duplicate guidance:** Write Workflow step 7 (file path constraint, 3 detailed lines) is essentially a condensed version of the full constraint in `references/write-actions.md`. Step 7 should be shortened to: "When creating an inline PR comment thread, MUST supply `--file-path` as a repo-root-relative path starting with `/`. See `references/write-actions.md` for full constraint details." Move the 3 lines of detail to the reference.
- No sections exceed 10 lines in code examples ✓

#### E — Rules

| Rule file | name | impact | description | apply-when | checks | anti-pattern | Issues |
|-----------|------|--------|-------------|------------|--------|--------------|--------|
| `read-only-boundary.md` | ✅ | critical ✅ | ✅ | ✅ | ✅ | ✅ | None |
| `write-boundary.md` | ✅ | critical ✅ | ✅ | ✅ | ✅ | ✅ | None |
| `normalization-precedence.md` | ✅ | high ✅ | ✅ | ✅ | ✅ | ✅ | "Inventing fallback values" anti-pattern is clear ✓ |

All three rules pass the weaker-model test: checks are concrete and binary. No missing fields.

#### F — References

| File | Referenced from SKILL.md | Critical-path content promoted? | Stale content? |
|------|--------------------------|----------------------------------|----------------|
| `workflow.md` | ✅ | No — detail only | ADO API version `7.1` hardcoded in write-actions.md |
| `write-actions.md` | ✅ | Partial — the `--confirm` gate is critical-path; already in SKILL.md ✓ | ADO api-version `7.1` (may need periodic updates) |
| `auth-and-safety.md` | ✅ | No — detail only | None |
| `normalization-rules.md` | ✅ | No — detail only | None |
| `handoff-contract.md` | ✅ | No — detail only | None |
| `end-to-end.md` | ✅ | No — example flow | None |
| `rule-order.md` | ✅ | No — ordering reference | None |

**Issue:** `references/write-actions.md` hardcodes `api-version=7.1`. Add a comment: `<!-- ADO API version — update if Azure DevOps REST API changes the stable version -->`.

#### G — Scripts

| Script | Responsibility | Header comment | Side-effect guard | Missing-dep handling | Issues |
|--------|----------------|----------------|-------------------|---------------------|--------|
| `ensure-env.sh` | Check deps + optional PAT | ✅ Full header | ✅ (exits with BLOCKER) | ✅ | None |
| `write-guard.sh` | Shared write helpers | ⚠️ Minimal header | ✅ | ✅ | Missing inputs/outputs documentation |
| `create-work-item.sh` | Create ADO work item | ✅ | ✅ dry-run default | ✅ via ensure-env.sh | None |
| `fetch-work-item.sh` | Fetch single work item | ✅ | N/A (read only) | ✅ | Uses `--retry-connrefused` but `write-guard.sh` curl_json_write lacks this flag — minor inconsistency |
| Other write scripts | Various write ops | Assumed ✅ | ✅ | ✅ | — |

**Issues:**
1. `write-guard.sh` has only `# Shared write helpers for ado-gateway write scripts.` — should add inputs, outputs, and side-effects header matching the format used in `ensure-env.sh`.
2. `fetch-work-item.sh` uses positional arguments (`$1`, `$2`, `$3`, `$4`) without named flags, unlike write scripts which use `--flag` style. Inconsistent UX; makes scripted invocation error-prone. Consider adding named-flag support.
3. The `run-validation.sh` harness is comprehensive (265 lines), covering dry-run shape, path validation, read-only boundary checks, and schema validation. ✅ This is a high-quality test harness.

#### H — Assets

| Asset | Used by skill | Schema strictness | Realistic/current? |
|-------|--------------|-------------------|--------------------|
| `schemas/ado-openspec-handoff.schema.json` | ✅ via validate-handoff.sh | ✅ `additionalProperties: false` throughout | ✅ |
| `templates/handoff.json` | ✅ | N/A — template | ✅ |
| `templates/blocker.txt` | ✅ | N/A | ✅ |
| `templates/error.txt` | ✅ | N/A | ✅ |
| `examples/*.json` | ✅ (validated by run-validation.sh) | Strict (schema-validated) | ✅ |

**Issue:** `pr_comments[*].file_path` schema pattern `"^\\/[^\\/].*"` applies only when the value is a non-null string. A null `file_path` (valid for general comment threads) correctly bypasses the pattern check — this behavior is intentional and safe. However, the pattern does not appear in any rule or comment explaining this edge case. Add a comment to the schema: `"// null for general comment threads; pattern enforced only for inline threads"`.

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states documented | ✅ | BLOCKER and ERROR formats defined in SKILL.md |
| Handoff to other skills explicit | ✅ | To spec-engine with contract defined |
| Environment requirements stated | ⚠️ | `curl`, `jq`, `python3` required (via ensure-env.sh) but not stated in SKILL.md; OS assumption (Linux/macOS bash) undocumented |
| HITL checkpoints defined | ✅ | Three explicit checkpoints in SKILL.md |

**Recommendations:**
1. Add an "Environment" section to SKILL.md: "Requires bash, curl, jq, and python3. Scripts assume a Linux or macOS environment."
2. Document the metadata.json/SKILL.md divergence on avoid-when items — sync them.

---

### `caveman`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `caveman` |
| Description has Use when / Do not use when | ✅ | Both present; ~35 words ✓ |
| `allowed-tools: []` | ✅ | Correct for a style-only skill |

**Issue:** The frontmatter `description` includes the constraint "after a domain skill is chosen" which conflicts with the `## Activate` section in the body, which lists `terse / less tokens / compress output` as direct triggers with no prerequisite domain skill. Decide which is authoritative and align them. Recommendation: remove "after a domain skill is chosen" from the description since the skill is also useful as the only active skill for quick queries.

#### B — Trigger Precision

**False negative risk (medium):** A user typing `/compress` or "keep it brief" might not match the trigger "after a domain skill is chosen" if no domain skill is loaded. The Activate section handles this more broadly — the frontmatter description should match.

**No overlap with other skills:** The style-layer nature of caveman is unique in the portfolio. ✓

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| "Switch to clear language first for: destructive actions..." | Should be "MUST switch to clear language" |
| "If ambiguity or confusion rises, move up one clarity level" | Should be "MUST move up one clarity level" |
| "Include `risk:` when action can break data..." | Should be "MUST include `risk:`" |

**Recommendation:** Audit every bullet in `## Safety override`, `## Risk + uncertainty`, and `## Coding-agent rules` and replace permissive language with MUST/NEVER where intent is unconditional.

#### D — Structure & Simplification

- SKILL.md: 157 lines — within limits ✓
- **Dead section:** `## Layout Note` (lines 100–102) explains why subdirectories are absent. This is package meta-commentary, not behavior-changing content. Move to README.md. `[REMOVE from SKILL.md]`
- **Near-duplicate guidance:** `## Do not use for` (lines 138–147) and the frontmatter `Do not use when:` cover the same territory. Consolidate: remove `## Do not use for` from the body and expand the frontmatter negative trigger with the body's additional cases (commit messages, PR titles, polished copy).
- The `## Abbreviations` section (24 lines) is reference-level content that could move to `assets/` if a references directory is ever added.

#### E — Rules

`caveman` is a `prompt`-type skill with no `rules/` directory — intentional and documented. ✅

#### F — References

No `references/` directory — intentional. ✅

#### G — Scripts

No `scripts/` directory — intentional. ✅

#### H — Assets

| Asset | Used by skill | Issues |
|-------|--------------|--------|
| `assets/templates/output.md` | ✅ (referenced in SKILL.md) | None |
| `assets/examples/happy-path.md` | ✅ | None |
| `assets/examples/edge-case.md` | ✅ | None |

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states documented | ✅ | Three-rule Error Handling section in SKILL.md |
| Handoff explicit | ℹ️ | No handoff — style layer has no domain handoff; acceptable |
| Environment requirements | ✅ | No tools required; no dependencies |
| HITL checkpoints | ✅ | Safety override effectively serves as HITL for dangerous content |

---

### `code-quality-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `code-quality-engine` |
| Description | ✅ | Triggers and negatives present; ~37 words ✓ |
| `allowed-tools: [read_file]` | ✅ | Read-only; correct |

#### B — Trigger Precision

**Overlap with `ops-engine`:** "Reviewing code for quality or safety" in code-quality-engine vs. reviewing CI/CD or deployment code in ops-engine. A PR containing a Dockerfile and application code could ambiguously route to either.

**Tie-breaker gap:** Add "Do not use when the review is for CI/CD pipelines, Dockerfiles, Kubernetes manifests, or deployment configuration — use ops-engine."

**Overlap with `test-engine`:** "Reducing complexity in fragile areas" could trigger when the fragility is test fragility. The Avoid When covers this: "Test design is the primary task." ✓

#### C — Rule Hardness

| Rule / Location | Soft language | Fix |
|-----------------|--------------|-----|
| Workflow step 1 | "if behavior is unclear, ask one question" | "MUST ask one question before proceeding" |
| Workflow step 3 | "Prefer the smallest safe change" | "MUST prefer the smallest safe change" |
| `minimal-mutation.md` Description | "Prefer narrow, surgical edits" | "MUST prefer narrow, surgical edits" |
| `correctness-first.md` Description | "Prioritize bugs, undefined behavior..." | "MUST prioritize" |
| `legacy-safety.md` Description | "Respect fragile legacy constraints" | "MUST respect fragile legacy constraints" |
| `performance-when-real.md` Description | "Treat performance recommendations as evidence-driven" | "MUST require profiling data or a measured bottleneck" |
| `complexity-reduction.md` Description | "Reduce branching, duplication..." | "MUST reduce branching..." |

#### D — Structure & Simplification

- SKILL.md: 92 lines — lean ✓
- **Pattern issue:** `rules/_sections.md` appears in 8 of 9 rule-type skills with an underscore prefix that makes it appear to be a meta/template file rather than an enforceable rule. Consider renaming to `standard-sections.md` across all skills to match the naming convention of all other rule files.
- `minimal-mutation.md` apply-when: "The code is fragile, legacy, or lightly tested" — leaves unaddressed what to do for modern, well-tested code. Add a note: "For well-tested code, prefer minimal mutation by default; relaxation requires explicit justification."

#### E — Rules

| Rule | Impact | Missing fields | Weaker-model clarity | Issues |
|------|--------|----------------|---------------------|--------|
| `_sections.md` | medium | None | ✅ | Naming inconsistency (underscore prefix vs. kebab-case) |
| `correctness-first.md` | critical | None | ✅ | Descriptions use soft verbs (see C) |
| `minimal-mutation.md` | high | None | ✅ | apply-when scope too narrow (legacy code only) |
| `complexity-reduction.md` | high | None | ✅ | Soft description language |
| `performance-when-real.md` | medium | None | ✅ | Soft description language |
| `legacy-safety.md` | high | None | ✅ | Soft description language |

#### F — References

| File | Referenced | Dead? | Critical-path content? |
|------|-----------|-------|----------------------|
| `workflow.md` | ✅ | No | No |
| `examples.md` | ✅ | No | No |

No staleness issues ✓. No dead references ✓.

#### G — Scripts

`validate-output.sh`: checks `findings:`, `patch-plan:`, `risk:` sections; validates severity labels and sort order. ✓

**Issue identified:** The severity sort check initializes `last_rank=4` (critical) and checks `rank > last_rank`. This correctly validates descending severity order. Logic is sound. ✓

#### H — Assets

Templates and examples referenced and exist. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ⚠️ | No explicit handoff defined. After a code review, the next step (implementing fixes) is not mapped to a skill |
| Environment | ✅ | `read_file` only; no network |
| HITL | ℹ️ | No destructive actions; no HITL required |

**Recommendation:** Add a `## Handoffs` section: "After surfacing findings and a patch plan, hand off to `test-engine` if new tests are needed, or to `delivery-engine` if the patch scope requires task decomposition."

---

### `delivery-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `delivery-engine` |
| Description | ✅ | ~40 words; triggers and negatives present ✓ |
| `allowed-tools: [read_file]` | ✅ | Read-only; correct |

#### B — Trigger Precision

**Overlap with `thinking-engine`:** "Estimating delivery scope before sprint commitment" could route here even when the scope itself is still unclear, which should route to thinking-engine first. The Avoid When says "Scope is too vague to produce atomic tasks — clarify before proceeding" but "too vague" is undefined.

**Recommendation:** Replace "Scope is too vague to produce atomic tasks — clarify before proceeding" with: "Do not use when acceptance criteria for every feature are absent — use thinking-engine or spec-engine first."

**Overlap with `spec-engine`:** "Breaking a spec, feature, or brief into executable tasks" — if the spec isn't yet written, this could double-trigger with spec-engine.

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 3 | "identify which dependencies are hard blockers versus soft ordering preferences" — soft imperative; should be "MUST distinguish" |
| Workflow step 4 | "mark any estimate as unreliable when the work is poorly understood" — good; add "MUST mark" |
| `critical-path.md` Checks | "The critical path is listed explicitly as an ordered chain of task IDs" ✓ — concrete enough |
| `estimate-integrity.md` | "No estimate is provided for a task that depends on an unresolved unknown — it is left blank or marked as pending" — good NEVER implicit ✓ |

**No conflicting rules detected.** Rules are well-differentiated.

#### D — Structure & Simplification

- SKILL.md: 111 lines ✓
- `rules/_sections.md` — same naming inconsistency (see code-quality-engine)

#### E — Rules

| Rule | Impact | Missing fields | Issues |
|------|--------|----------------|--------|
| `_sections.md` | medium | None | Naming inconsistency |
| `vertical-slicing.md` | critical | None | ✅ Anti-pattern example is concrete and useful |
| `unknown-exposure.md` | critical | None | ✅ "If an unknown blocks all tasks, the plan stops" — strong ✓ |
| `estimate-integrity.md` | high | None | ✅ Very specific checks |
| `critical-path.md` | high | None | ✅ "If all tasks are independent, state that explicitly" — good edge case ✓ |

All rules pass weaker-model test. ✓

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | None |
| `examples.md` | ✅ | None |

#### G — Scripts

`validate-output.sh`: checks 6 required sections, done_when count parity, estimate/timebox count parity. Logic is correct. ✓

#### H — Assets

Referenced and present. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ⚠️ | Consumes spec-engine ✓; handoff out says "implementation or review skills" without naming them |
| Environment | ✅ | read_file only |
| HITL | ℹ️ | No destructive actions |

**Recommendation:** In the `## Handoffs` section, name the downstream skills explicitly: "...hand off to `code-quality-engine`, `test-engine`, `doc-engine`, or `ops-engine` depending on whether the next work surface is code, tests, documentation, or operations."

---

### `doc-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `doc-engine` |
| Description | ✅ | ~45 words; both trigger types present ✓ |
| `allowed-tools: [read_file]` | ✅ | Correct; no writes via tools |

#### B — Trigger Precision

**Well-disambiguated against repo-engine:** The avoid-when on repo-engine says "the request is to author onboarding or AGENTS.md docs" ✓. The avoid-when on doc-engine says "when the primary need is repository discovery" ✓.

**Possible false positive:** "Explaining how a system should be used or maintained" could trigger for inline code comment writing, which is more naturally part of code-quality-engine's scope. Add: "Do not use for inline code comments or docstrings."

**False negative:** Decision records (ADRs) are supported but not in the frontmatter description. Only in the SKILL.md body ("Capturing an architectural decision..."). Move this trigger to the description.

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 4 | "Keep commands, file paths, and workflow steps accurate; verify each before including" — "verify each" is vague; no method specified |
| Workflow step 3 | "default to a diff-only update; stop and request explicit approval before replacing" — imperative ✓ |
| `command-accuracy.md` Checks | "Commands are verified against the current repo or toolchain" — no definition of HOW to verify |

**Recommendation:** Add to workflow step 4: "MUST verify each command by checking it against an existing file in the repository or against the output of running it in the current environment."

#### D — Structure & Simplification

- SKILL.md: 105 lines ✓
- `rules/_sections.md` — same naming inconsistency
- `rules/diataxis-mode.md` is the longest rule file in the portfolio (~53 lines). The "## The Four Modes" section (30 lines of mode descriptions) is reference-level content. Move the four-mode descriptions to `references/` and keep only the tag tokens and apply-when/checks/anti-pattern in the rule file.
- `rules/audience-first.md` and `rules/structure-over-prose.md` both cross-reference `rules/diataxis-mode.md` in their checks — this chaining is acceptable but means the rules cannot be applied independently.

#### E — Rules

| Rule | Impact | Format compliance | Issues |
|------|--------|-------------------|--------|
| `_sections.md` | medium | ✅ | Naming inconsistency |
| `audience-first.md` | critical | ✅ | Cross-references diataxis-mode ✓ |
| `command-accuracy.md` | high | ✅ | No verification method defined |
| `structure-over-prose.md` | medium | ✅ | Cross-references diataxis-mode ✓ |
| `agentsmd-specificity.md` | high | ✅ | Concise and testable ✓ |
| `readme-completeness.md` | high | ✅ | "Omitted details are intentional and not critical" — hard to test |
| `decision-capture.md` | high | ✅ | Extended format (also has `## When Not to Apply`, `## Output Format`) — effectively a mini-reference; acceptable given its complexity |
| `diataxis-mode.md` | high | ✅ | Over-long for a rule file; "The Four Modes" section should move to references/ |

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | None |
| `examples.md` | ✅ | None |

#### G — Scripts

`validate-output.sh` — exists and referenced. ✓

#### H — Assets

5 example files in `assets/examples/` — more than other skills. The additional files (`decision-record.md`, `mode-mixed.md`) map directly to the decision-capture and diataxis-mode rules, which is good alignment. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ℹ️ | Doc-engine is typically terminal; acceptable |
| Environment | ✅ | read_file only |
| HITL checkpoints | ✅ | Overwrite approval checkpoint defined ✓ |

---

### `ops-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `ops-engine` |
| Description | ✅ | ~38 words; triggers and negatives ✓ |
| `allowed-tools: [read_file]` | ⚠️ | The skill produces executable check commands but doesn't run them; read_file is correct if ops-engine is plan-only |

**Note:** The skill produces "checks: [concrete, executable commands]" as output text, not by running them. The `allowed-tools: [read_file]` is therefore correct. However, the validation checklist says "Every check is a concrete, executable command" — it would be helpful to clarify in SKILL.md that checks are described, not executed by this skill.

#### B — Trigger Precision

**Overlap with `code-quality-engine`:** Security review of application code. A request to "review this authentication code for security issues" could route to either.

**Tie-breaker:** Add to avoid-when: "Do not use when the security concern is in application code logic (SQL injection, auth bypasses, input validation) — use code-quality-engine."

**Overlap with `thinking-engine`:** "Designing resilience, rollback, or release safety plans" vs. thinking-engine's "plan needs critique before commitment." Use thinking-engine when no concrete infrastructure is involved.

#### C — Rule Hardness

| Rule / Location | Soft language | Fix |
|-----------------|--------------|-----|
| `resilience-rollback.md` Description | "Every operational plan **should** define failure handling" | Replace "should" with "MUST" — this is a CRITICAL impact rule |
| `deployment-safety.md` Description | "Prioritize safe rollout..." | "MUST include staging, gating, or progressive rollout" |
| `kubernetes-safety.md` Description | "Favor least privilege..." | "MUST apply least privilege and resource discipline" |
| Workflow step 5 | "stop for human approval before any irreversible production step" | Good — imperative ✓ |

**`resilience-rollback.md` uses "should" in a CRITICAL-impact rule.** This is the most significant hardness failure in the portfolio. A CRITICAL rule that says "should" will be ignored by weaker models.

#### D — Structure & Simplification

- SKILL.md: 101 lines ✓
- `rules/_sections.md` — same naming inconsistency

#### E — Rules

| Rule | Impact | Issues |
|------|--------|--------|
| `_sections.md` | medium | Naming inconsistency |
| `deployment-safety.md` | critical | ✅ |
| `ci-cd-reproducibility.md` | high | ✅ |
| `kubernetes-safety.md` | high | ✅ |
| `threat-model-boundaries.md` | high | ✅ |
| `resilience-rollback.md` | critical | ⚠️ Uses "should" in a CRITICAL rule — must be fixed |

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | None |
| `examples.md` | ✅ | None |

#### G — Scripts

`validate-output.sh`: checks risk/plan/checks/rollback/security sections; checks BLOCKED/ABORT pattern; checks production/human-approval gate.

**Issue:** The BLOCKED check uses `grep -qiE "BLOCKED"` — case-insensitive. "blocked port," "blocked request," or "is blocked by a firewall" in a normal plan description would trigger a false failure requiring an ABORT statement. Narrow the pattern to `"BLOCKER:"` or `"plan: BLOCKED"` to prevent false positives.

#### H — Assets

Referenced and present. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ⚠️ | No downstream handoff defined |
| Environment | ✅ | read_file only; checks are text outputs |
| HITL checkpoints | ✅ | Three explicit production/destructive checkpoints ✓ |

---

### `repo-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `repo-engine` |
| Description | ✅ | ~45 words; well-disambiguated ✓ |
| `allowed-tools: [list_dir, read_file]` | ✅ | Only skill with `list_dir`; appropriate |

#### B — Trigger Precision

**Well-disambiguated from doc-engine** ✓

**Avoid-when gap:** "Only code quality review is needed" — this should name the receiving skill: "Only code quality review is needed — use code-quality-engine."

**False positive risk (low):** "Understanding architecture before making changes" could activate even for a small one-file change. Add: "Do not use when the relevant files are already known and the change surface is fewer than 5 files."

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 4 | "Highlight hotspots" — should be "MUST highlight hotspots" |
| `convention-extraction.md` | "Derive conventions from repeated evidence" — should be "MUST derive" |
| `hotspot-identification.md` | "Highlight modules or workflows that are risky" — should be "MUST explain why each hotspot is risky" |
| Workflow step 1 | "if access is restricted, note the gap and continue" — good fallback ✓ |

#### D — Structure & Simplification

- SKILL.md: 100 lines ✓
- `rules/_sections.md` — same naming inconsistency

#### E — Rules

| Rule | Impact | Issues |
|------|--------|--------|
| `_sections.md` | medium | Naming inconsistency |
| `architecture-map.md` | high | ✅ |
| `entrypoint-detection.md` | high | ✅ |
| `convention-extraction.md` | medium | Soft description language |
| `hotspot-identification.md` | medium | Soft description language |

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | None |
| `artifact-schema.md` | ✅ | JSON schema present — more formal than other skills ✓ |
| `examples.md` | ✅ | None |

**Strength:** `references/artifact-schema.md` provides a JSON schema for the output contract — the only non-ado-gateway skill with a formal output schema. Other skills should consider adopting this pattern.

#### G — Scripts

`validate-output.sh` — exists. ✓

#### H — Assets

Referenced and present. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ✅ | Five downstream skills named explicitly ✓ |
| Environment | ✅ | list_dir + read_file; no network |
| HITL | ✅ | No destructive actions; N/A |

**Strength:** repo-engine's handoffs section is the most explicit in the portfolio. Model for other skills. ✓

---

### `spec-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `spec-engine` |
| Description | ✅ | ~45 words; includes ado-gateway handoff and OpenSpec triggers ✓ |
| `allowed-tools: [read_file]` | ✅ | Correct |

#### B — Trigger Precision

**Near-duplicate trigger in Use When:** "Creating a freeform implementation-ready spec from user input, tickets, or research" and "Producing implementation-ready artifacts from ambiguous requests" overlap significantly. Consolidate into one.

**Overlap with `thinking-engine`:** "Producing implementation-ready artifacts from ambiguous requests" — ambiguous requests should route to thinking-engine first. The Avoid When says "The task has no requirement or spec component" but this doesn't guard against the ambiguous-request scenario.

**Recommendation:** Add to Avoid When: "Do not use when the problem is still exploratory and no concrete requirements exist — use thinking-engine first."

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 3 (duplicate "3") | Numbering bug: two items labeled "3" and "3a" — renumber to 3 and 4 |
| Workflow step 3 | "Check for ambiguity..." — should be "MUST check for" |
| `acceptance-criteria.md` | Does not enforce GIVEN/WHEN/THEN explicitly in checks — the validation checklist in SKILL.md does, but the rule doesn't |
| `diff-versioning.md` | "Material scope or contract changes are called out explicitly" — "called out" is soft; should be "MUST document" |

#### D — Structure & Simplification

- SKILL.md: 126 lines ✓
- Workflow step numbering bug (two items numbered "3") must be fixed — this will confuse any model following the numbered steps.
- `rules/openspec-format.md` and `references/openspec-format.md` coexist with different content (rule vs. reference). Both are referenced. The distinction is maintained cleanly. ✓

#### E — Rules

| Rule | Impact | Issues |
|------|--------|--------|
| `_sections.md` | medium | Naming inconsistency; adds `risk_tier` check ✓ |
| `requirement-quality.md` | critical | ✅ Banned words list is actionable and concrete ✓ |
| `acceptance-criteria.md` | high | Missing GIVEN/WHEN/THEN enforcement in checks |
| `diff-versioning.md` | medium | Soft language in checks |
| `policy-gates.md` | high | ✅ |
| `handoff-consumption.md` | critical | ⚠️ Missing `## Description` and `## Checks` headers — content is present but structure deviates from all other rule files in the portfolio |
| `openspec-format.md` | high | ✅ References OpenSpec v1.3.1 SHA pins |

**`handoff-consumption.md` structural issue:** The rule file starts directly with constraint bullets rather than following the `## Description` / `## Apply When` / `## Checks` / `## Anti-Pattern` structure used by all other rules. Restructure to match.

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | None |
| `artifact-schema.md` | ✅ | Includes JSON schema ✓ |
| `openspec-format.md` | ✅ | Hardcodes OpenSpec v1.3.1 with SHA pins |
| `examples.md` | ✅ | Very brief (3 prose examples, no structured output) |

**Issue:** `references/openspec-format.md` pins OpenSpec to `v1.3.1` and provides two SHAs. Add a comment block: `<!-- Update version pin and SHAs when the upstream OpenSpec project releases a new stable version -->`. The SHA pins are good for reproducibility but the maintenance requirement is not documented.

#### G — Scripts

`validate-output.sh` — exists. ✓

#### H — Assets

Referenced and present. The `assets/examples/happy-path.md` (password reset spec scenario) and `edge-case.md` (conflicting requirements scenario) are tightly aligned with the requirement-quality and acceptance-criteria rules. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Four error handling rules including overwrite protection |
| Handoff | ✅ | Both consuming (ado-gateway) and producing (delivery-engine) handoffs documented ✓ |
| Environment | ✅ | read_file only |
| HITL checkpoints | ✅ | Spec overwrite requires explicit approval ✓ |

---

### `test-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `test-engine` |
| Description | ✅ | ~38 words; triggers and negatives ✓ |
| `allowed-tools: [read_file]` | ✅ | Correct |

#### B — Trigger Precision

**Well-disambiguated from code-quality-engine:** code-quality-engine explicitly avoids "test quality or coverage review." ✓

**Possible false positive:** "Reducing flaky or brittle tests" is a code maintenance task that could route here even when the underlying issue is a timing bug (which would be a code-quality-engine issue). Low risk.

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 2 | "Choose the lowest-cost test level that proves the behavior" — should be "MUST choose" |
| Workflow step 4 | "Keep E2E tests stable by controlling timing, data, and selectors" — should be "MUST control" |
| `behavior-over-implementation.md` | "Prefer tests that verify externally visible behavior" — should be "MUST prefer" |
| `meaningful-coverage.md` | "Increase coverage where it reduces risk" — should be "MUST focus coverage on highest-risk paths" |
| `flaky-test-reduction.md` | "Treat flakiness as a product defect" — framing language; replace with "MUST identify the source of non-determinism before adding any retry" |
| `e2e-stability.md` | "Prefer user-facing, stable selectors" — should be "MUST prefer" |

#### D — Structure & Simplification

- SKILL.md: 92 lines ✓
- `rules/_sections.md` — same naming inconsistency

#### E — Rules

| Rule | Impact | Issues |
|------|--------|--------|
| `_sections.md` | medium | Naming inconsistency |
| `behavior-over-implementation.md` | critical | ✅ Anti-pattern (DOM structure, private methods) is concrete ✓ |
| `meaningful-coverage.md` | high | ✅ |
| `edge-cases.md` | high | ✅ |
| `e2e-stability.md` | high | Soft description language |
| `flaky-test-reduction.md` | high | Soft framing language |

**Overlap between `e2e-stability.md` and `flaky-test-reduction.md`:** E2E stability focuses on design-time decisions; flaky reduction focuses on diagnosis of existing tests. The apply-when conditions differentiate them sufficiently. ✓

#### F — References

`workflow.md` and `examples.md` — both referenced. ✓

#### G — Scripts

`validate-output.sh` — exists. ✓

#### H — Assets

Referenced and present. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ⚠️ | No downstream handoff defined |
| Environment | ✅ | read_file only |
| HITL | ℹ️ | No destructive actions |

**Recommendation:** Add a `## Handoffs` section: "After producing a test plan, hand off to `code-quality-engine` if implementation changes are needed to make tests pass, or to `delivery-engine` if the test coverage work is large enough to require task decomposition."

---

### `thinking-engine`

#### A — Frontmatter Hygiene

| Check | Status | Detail |
|-------|--------|--------|
| `name` matches directory | ✅ | `thinking-engine` |
| Description | ✅ | ~38 words; triggers and negatives ✓ |
| `allowed-tools: []` | ✅ | Pure reasoning; no tools needed ✓ |

#### B — Trigger Precision

**Overlap with `delivery-engine`:** "MVP scope validation before execution" in thinking-engine vs. "Estimating delivery scope before sprint commitment" in delivery-engine. The boundary is: thinking-engine questions whether the scope is right; delivery-engine breaks confirmed scope into tasks. This boundary is implicit, not stated.

**Recommendation:** Add to avoid-when in thinking-engine: "Do not use when scope is confirmed and the output is an executable task list — use delivery-engine."

The frontmatter description already says this: "Do not use when scope is fixed and the output must be an executable task breakdown with dependencies and done criteria." ✓

#### C — Rule Hardness

| Location | Issue |
|----------|-------|
| Workflow step 3 | "Generate a small set of materially different options" — "small" is vague; specify "2–4" |
| `assumption-audit.md` | "Call out constraints that could change the preferred option" — should be "MUST call out" |
| `option-generation.md` | "when tradeoffs exist" in apply-when — vague; should be "when two or more technically viable approaches exist" |
| `decision-readiness.md` | "State the recommended path and why it is preferred now" — should be "MUST state" |

#### D — Structure & Simplification

- SKILL.md: 98 lines ✓
- `rules/_sections.md` — same naming inconsistency
- **Undocumented fast path:** `references/workflow.md` introduces a "Fast Path" (2-step abbreviated workflow) and an "Escalation" mode not mentioned anywhere in SKILL.md. These modes materially change agent behavior — they should be surfaced in SKILL.md or explicitly linked: "See `references/workflow.md` for fast path and escalation modes."

#### E — Rules

| Rule | Impact | Issues |
|------|--------|--------|
| `_sections.md` | medium | Naming inconsistency |
| `assumption-audit.md` | critical | Soft description language |
| `option-generation.md` | high | Vague apply-when |
| `decision-readiness.md` | high | Soft description language |

#### F — References

| File | Referenced | Issues |
|------|-----------|--------|
| `workflow.md` | ✅ | ⚠️ Introduces fast path and escalation modes not in SKILL.md — promote to SKILL.md |
| `examples.md` | ✅ | None |

#### G — Scripts

`validate-output.sh`: checks 5 sections and at least 1 numbered option.

**Issue:** The option count check `grep -cE '^[0-9]+\. '` matches any numbered list in the output, not just the `options:` section. A plan with a numbered `next:` section and no options listed under `options:` would satisfy the check incorrectly. Narrow the regex to match only within the options block, or require at least 2 distinct options.

#### H — Assets

Referenced and present. ✓

#### I — Package-Level: Safety & Completeness

| Check | Status | Detail |
|-------|--------|--------|
| Error states | ✅ | Three error handling rules |
| Handoff | ✅ | To spec-engine and delivery-engine ✓ |
| Environment | ✅ | No tools needed |
| HITL | ℹ️ | No destructive actions |

---

## Step 3 — Cross-Skill Analysis

### Description overlaps that could cause ambiguous routing

| Skill A | Skill B | Overlapping description element | Tie-breaker status | Recommended fix |
|---------|---------|----------------------------------|-------------------|----------------|
| `thinking-engine` | `spec-engine` | "Producing implementation-ready artifacts from ambiguous requests" (spec-engine) | ❌ Missing tie-breaker in spec-engine | Add to spec-engine avoid-when: "Do not use when the problem is exploratory with no concrete requirements — use thinking-engine first" |
| `code-quality-engine` | `ops-engine` | Security review of application code vs. infrastructure threat modeling | ❌ Missing tie-breaker in both | Add to ops-engine avoid-when: "Do not use when the security concern is in application logic — use code-quality-engine"; add to code-quality-engine avoid-when: "Do not use for CI/CD, Dockerfiles, or Kubernetes manifests — use ops-engine" |
| `thinking-engine` | `delivery-engine` | MVP scope validation vs. estimating delivery scope | ⚠️ Partial — delivery-engine avoid-when covers "scope is too vague" but definition of "too vague" is undefined | Replace "too vague" with concrete condition: "acceptance criteria absent" |
| `repo-engine` | `doc-engine` | Producing agent-ready onboarding artifacts vs. writing AGENTS.md | ✅ Disambiguated on both sides | — |
| `spec-engine` | `test-engine` | Validating acceptance criteria | ❌ Not covered in either avoid-when | Add to spec-engine: "Do not use for reviewing test case quality — use test-engine"; add to test-engine: "Do not use for writing new requirements or acceptance criteria — use spec-engine" |

### Skill chain audit

| Chain | Documented on producer | Documented on consumer | Status |
|-------|----------------------|----------------------|--------|
| `ado-gateway` → `spec-engine` | ✅ (Handoffs section) | ✅ (Handoffs section) | Complete |
| `spec-engine` → `delivery-engine` | ✅ | ✅ | Complete |
| `thinking-engine` → `spec-engine` | ✅ | ❌ spec-engine doesn't mention accepting from thinking-engine | One-sided; add to spec-engine |
| `thinking-engine` → `delivery-engine` | ✅ | ❌ delivery-engine doesn't mention accepting from thinking-engine | One-sided; add to delivery-engine |
| `repo-engine` → `code-quality-engine` | ✅ | ❌ code-quality-engine has no Handoffs section | One-sided; add to code-quality-engine |
| `repo-engine` → `test-engine` | ✅ | ❌ test-engine has no Handoffs section | One-sided; add to test-engine |
| `repo-engine` → `doc-engine` | ✅ | ❌ doc-engine has no Handoffs section | One-sided; add to doc-engine |
| `repo-engine` → `ops-engine` | ✅ | ❌ ops-engine has no Handoffs section | One-sided; add to ops-engine |
| `delivery-engine` → (implementation skills) | ⚠️ vague ("implementation or review skills") | N/A | Improve delivery-engine: name specific skills |

### Missing skills (coverage gaps)

| Gap | Description | Nearest existing skill | Why a new skill is justified |
|-----|-------------|----------------------|------------------------------|
| **github-pr-gateway** | Fetching, normalizing, and commenting on GitHub PRs | `ado-gateway` (Avoid When: "GitHub PR review") | `ado-gateway` explicitly excludes GitHub; there is no equivalent gateway for GitHub PR data |
| **security-audit** | Application-level security review (OWASP, injection, auth bypass, secrets scanning) | `code-quality-engine` (mentions "security-adjacent defects") | Application security is a deep domain; `code-quality-engine` handles it incidentally and `ops-engine` handles it at the infrastructure level only; neither owns it |
| **dependency-audit** | Dependency vulnerability scanning, license compliance, supply-chain risk | None | No skill covers this surface; frequently needed before deployment |

---

## Summary Table — All Findings

| Skill | Section | Severity | Issue |
|-------|---------|----------|-------|
| `ado-gateway` | A | low | "narrowly creating approved" is ambiguous in frontmatter description |
| `ado-gateway` | A | low | `metadata.json` and SKILL.md avoid-when sections differ (reviewer votes omitted in metadata) |
| `ado-gateway` | B | low | Avoid-when missing: updating/patching existing work items is unsupported but not listed |
| `ado-gateway` | C | low | Write Workflow step 7: missing MUST before "supply `--file-path`" |
| `ado-gateway` | C | low | normalization-precedence.md: "win over" should be "MUST take precedence over" |
| `ado-gateway` | D | low | Write Workflow step 7 duplicates write-actions.md — trim step 7 and refer to reference |
| `ado-gateway` | F | medium | `references/write-actions.md` hardcodes `api-version=7.1` with no update reminder comment |
| `ado-gateway` | G | low | `write-guard.sh` missing full inputs/outputs/side-effects header |
| `ado-gateway` | G | low | `fetch-work-item.sh` uses positional args; write scripts use named flags — UX inconsistency |
| `ado-gateway` | H | low | Schema `file_path` null-bypass behavior undocumented; add inline comment |
| `ado-gateway` | I | medium | No OS/shell environment requirements stated in SKILL.md |
| `caveman` | A | medium | Frontmatter "after a domain skill is chosen" contradicts body Activate section which lists direct triggers |
| `caveman` | C | medium | Safety override and risk sections use soft language (Switch, Include) instead of MUST |
| `caveman` | D | low | `## Layout Note` is meta-commentary — move to README.md |
| `caveman` | D | low | `## Do not use for` duplicates frontmatter negative triggers — consolidate |
| `code-quality-engine` | B | medium | No tie-breaker for security review of app code vs. ops-engine security review |
| `code-quality-engine` | C | medium | Rules use "Prioritize", "Prefer", "Respect", "Treat", "Reduce" — replace with MUST throughout |
| `code-quality-engine` | C | low | Workflow step 1 "ask one question" missing MUST |
| `code-quality-engine` | D | low | `_sections.md` underscore prefix naming inconsistency (all skills affected) |
| `code-quality-engine` | E | medium | `minimal-mutation.md` apply-when restricted to "fragile, legacy, or lightly tested" — misses all other code |
| `code-quality-engine` | I | medium | No Handoffs section — next steps after code review are undefined |
| `delivery-engine` | B | medium | "Scope is too vague" in avoid-when is undefined — specify concrete condition |
| `delivery-engine` | C | low | Workflow step 3 "identify dependencies" missing MUST |
| `delivery-engine` | I | low | Handoffs out vague — "implementation or review skills" should name specific skills |
| `doc-engine` | B | low | "Explaining how a system should be used" could trigger for inline code comments — add negative trigger |
| `doc-engine` | B | low | Decision record trigger only in body, not frontmatter description |
| `doc-engine` | C | medium | Workflow step 4 "verify each" has no defined verification method |
| `doc-engine` | E | medium | `diataxis-mode.md` over-long for a rule (53 lines); "The Four Modes" section should move to references/ |
| `doc-engine` | E | low | `audience-first.md` and `structure-over-prose.md` cannot be applied independently (cross-reference to diataxis-mode) |
| `doc-engine` | E | low | `readme-completeness.md` check "Omitted details are intentional" is not mechanically verifiable |
| `ops-engine` | B | medium | No tie-breaker for application-code security review vs. code-quality-engine |
| `ops-engine` | C | high | `resilience-rollback.md` is CRITICAL impact but uses "should" — must be replaced with MUST |
| `ops-engine` | C | medium | `deployment-safety.md`: "Prioritize safe rollout" — should be "MUST include" |
| `ops-engine` | C | medium | `kubernetes-safety.md`: "Favor least privilege" — should be "MUST apply least privilege" |
| `ops-engine` | G | medium | `validate-output.sh` BLOCKED pattern `grep -qiE "BLOCKED"` generates false positives on routine text |
| `ops-engine` | I | low | No downstream handoff defined |
| `repo-engine` | B | low | Avoid-when "Only code quality review is needed" doesn't name the receiving skill |
| `repo-engine` | B | low | No size threshold for triggering repo scan ("fewer than 5 files" scenario) |
| `repo-engine` | C | low | `convention-extraction.md` and `hotspot-identification.md` use soft description language |
| `spec-engine` | B | medium | "Producing implementation-ready artifacts from ambiguous requests" overlaps with thinking-engine |
| `spec-engine` | B | low | Two Use When items are near-duplicates — consolidate |
| `spec-engine` | C | high | Workflow step numbering bug: two items labeled "3" and "3a" |
| `spec-engine` | C | medium | `acceptance-criteria.md` does not enforce GIVEN/WHEN/THEN in rule checks |
| `spec-engine` | C | low | `diff-versioning.md` checks use soft "called out" language |
| `spec-engine` | E | medium | `handoff-consumption.md` deviates from standard rule structure (no `## Description` / `## Checks` headers) |
| `spec-engine` | F | medium | `references/openspec-format.md` pins OpenSpec v1.3.1 with no update reminder |
| `spec-engine` | F | low | `references/examples.md` is very brief (3 prose paragraphs with no structured output) |
| `test-engine` | C | medium | Rules use "Prefer", "Treat", "Increase" — replace with MUST throughout |
| `test-engine` | C | low | Workflow step 2 "Choose" missing MUST |
| `test-engine` | I | medium | No Handoffs section |
| `thinking-engine` | C | medium | Workflow step 3 "small set" is undefined — specify 2–4 options |
| `thinking-engine` | C | medium | `assumption-audit.md`, `option-generation.md`, `decision-readiness.md` use soft description language |
| `thinking-engine` | D/F | medium | `references/workflow.md` introduces fast path and escalation modes not documented in SKILL.md |
| `thinking-engine` | G | medium | `validate-output.sh` option count check matches any numbered list, not only the `options:` section |
| **Cross-skill** | Routing | high | thinking-engine/spec-engine overlap on ambiguous-requests trigger — missing tie-breaker in spec-engine |
| **Cross-skill** | Routing | high | code-quality-engine/ops-engine security review overlap — missing tie-breakers on both sides |
| **Cross-skill** | Chains | medium | thinking-engine → spec-engine handoff undocumented on spec-engine side |
| **Cross-skill** | Chains | medium | thinking-engine → delivery-engine handoff undocumented on delivery-engine side |
| **Cross-skill** | Chains | medium | repo-engine → (code-quality, test, doc, ops) handoffs undocumented on consumer side |
| **Cross-skill** | Chains | low | delivery-engine → downstream skills vague ("implementation or review skills") |
| **Cross-skill** | Gaps | high | No skill covers GitHub PR fetching/review |
| **Cross-skill** | Gaps | high | No skill covers application-level security audit |
| **Cross-skill** | Gaps | medium | No skill covers dependency/supply-chain vulnerability audit |
| **Portfolio** | Convention | low | `_sections.md` underscore prefix is inconsistent with all other kebab-case rule filenames — rename to `standard-sections.md` across all skills |
| **Portfolio** | Convention | low | `metadata.json` and `README.md` are non-canonical layout files present in all skills; not documented in the canonical layout spec — document or adopt officially |

---

## Severity Legend

| Severity | Meaning |
|----------|---------|
| **high** | Blocks correct behavior or creates routing errors; fix before next skill release |
| **medium** | Degrades output quality or creates routing risk for weaker models; fix in next planned revision |
| **low** | Clarity, consistency, or maintenance concern; fix opportunistically |

---

## Top Priorities by Severity

### High severity (fix immediately)

1. **`ops-engine` / `resilience-rollback.md`** — CRITICAL-impact rule uses "should" instead of "MUST." A model following this rule literally will treat rollback planning as optional.
2. **`spec-engine` / Workflow step numbering** — Duplicate "3" and "3a" step labels; weaker models will missequence the workflow.
3. **Cross-skill: thinking-engine/spec-engine routing** — Ambiguous-request trigger in spec-engine will capture exploratory problems that should route to thinking-engine first.
4. **Cross-skill: code-quality-engine/ops-engine security** — No skill owns application-level security review; requests will route ambiguously.
5. **Missing GitHub PR gateway** — `ado-gateway` explicitly excludes GitHub; no skill covers this surface.

### Medium severity (fix in next revision)

6. **Rule language hardness** — `code-quality-engine`, `test-engine`, `ops-engine`, `thinking-engine` rules use "Prefer," "Treat," "Favor" in descriptions. Systematically replace with MUST/NEVER.
7. **`_sections.md` naming** — Rename to `standard-sections.md` across all 8 skills for naming consistency.
8. **Missing Handoffs sections** — `code-quality-engine`, `test-engine`, `ops-engine`, `doc-engine` have no downstream handoffs defined. All four skills are reached via repo-engine or other skills but don't document what happens next.
9. **`ops-engine` validate-output.sh BLOCKED false positive** — Narrow regex to prevent ordinary plan text from triggering ABORT requirement.
10. **`thinking-engine` validate-output.sh** — Fix option count check to scope within `options:` section only.
11. **`spec-engine` `handoff-consumption.md`** — Restructure to standard rule format with `## Description` and `## Checks` headers.
12. **`diataxis-mode.md` length** — Move "The Four Modes" descriptions to `references/` and trim the rule file to checks + anti-pattern.
