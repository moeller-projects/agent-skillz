{
  "name": "{{skill-name}}",
  "title": "{{Skill Title}}",
  "version": "{{version}}",
  "summary": "{{summary}}",
  "type": "hybrid",
  "activation": {
    "use_when": [
      "{{use-when}}"
    ],
    "avoid_when": [
      "{{avoid-when}}"
    ]
  }
}
