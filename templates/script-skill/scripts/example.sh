#!/usr/bin/env bash
# example.sh — starter script for {{skill-name}}
# Replace this with your skill's actual helper logic.

set -euo pipefail

# Write status output to stderr so stdout stays available for real script results.
echo "Running {{skill-name}} script..." >&2
