{
  "name": "{{skill-name}}",
  "title": "{{Skill Title}}",
  "version": "{{version}}",
  "summary": "{{summary}}",
  "type": "prompt",
  "activation": {
    "use_when": [
      "{{use-when}}"
    ],
    "avoid_when": [
      "{{avoid-when}}"
    ]
  }
}
